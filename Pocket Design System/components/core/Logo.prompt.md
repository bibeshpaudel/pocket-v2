The Pocket logo — amber mark plus optional wordmark; use in headers and footers, never recolor it.

```jsx
<Logo size={28} />          // mark + "Pocket"
<Logo size={32} wordmark={false} />  // mark only
```
